.contents__groups
  - current_user.groups.each do |group|
    .contents__groups__box
      = link_to groups_messages_path(group) do
        %h3
          =group.name
        %p
          メッセージはまだありません。
