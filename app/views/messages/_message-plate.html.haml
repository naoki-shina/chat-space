.contents__main__ajax__messagebox{ "data-message-id": message.id}
  .user_name
    = message.user.name
  .date
    = message.created_at
  .message
    - if message.body.present?
      %p.message__body
        = message.body
    =image_tag message.image.url, class: "image" if message.image.present?
