.contents__main__messagebox
  / - @messages.each do |message|
  .user_name
    = message.user.name
  .date
    = message.created_at
  .message
    - if message.body.present?
      %p.message__body
        = message.body
    =image_tag message.image.url, class: "image" if message.image.present?
